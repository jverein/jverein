src.dir                         = src
build.dir                       = build
icon.dir                        = ${src.dir}/img
lang.dir                        = ${src.dir}/lang
help.dir                        = ${src.dir}/help
lib.dir                         = lib
sql.dir                         = sql
sql.mckoi.dir                   = sql.mckoi
sql.h2.dir                      = sql.h2
sql.mysql.dir                   = sql.mysql
class.dir                       = bin

define.pluginname               = jverein
define.jarfilename              = ${define.pluginname}.jar
define.srcfilename              = ${define.pluginname}.src.zip
define.package                  = de.jost_net.JVerein

project.release                 = releases/${version}-${build.number}
project.src                     = ${project.release}/src/classes
project.tmp                     = ${project.release}/tmp
project.javadoc                 = ${project.release}/javadoc
project.zipdir                  = ${project.release}/${define.pluginname}
project.zipfilename             = ${define.pluginname}.${version}-${build.number}.zip
